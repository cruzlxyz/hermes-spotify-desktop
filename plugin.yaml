name: spotify-desktop
version: 0.1.0
description: "Spotify Connect remote — backend API (dashboard/plugin_api.py) for the desktop status-bar player. No agent tools; the agent half exists so the desktop installer recognizes the unified package."
author: "cruzlxyz"
kind: backend
